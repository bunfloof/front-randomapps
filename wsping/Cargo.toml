[package]
name = "wsping"
version = "0.1.0"
edition = "2024"

[dependencies]
futures-util = "0.3.31"
tokio = { version = "1.48.0", features = ["rt-multi-thread", "macros", "net", "time"] }
tokio-tungstenite = "0.28.0"

[profile.release]
lto = true
strip = true